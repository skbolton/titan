name: GENESIS SERVER
root: ~/Public/genesis-server

windows:
  - " ": nvim
  - " ":
      layout: main-vertical
      panes:
        - tmux link-window -s spotify:"阮" -d
        - tmux link-window -s Notes -d
        - docker-compose up

