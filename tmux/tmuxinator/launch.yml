name: <%= @settings["name"].split(/[-_]/).map(&:capitalize).join(' ') %>
root: <%= @settings["root"] %>

windows:
  - " ": v
  - " ": tmux link-window -s spotify:" "; tmux select-window -t <%= @settings["name"] %>: ; clear
